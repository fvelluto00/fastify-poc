receivers:
  otlp:
    protocols:
      http:

exporters:
  otlphttp:
    endpoint: "http://jaeger:14268/api/traces"

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp]
